dnspython>=2.4.0
python-whois>=0.9.4
requests>=2.31.0
# optional — full cryptographic DKIM verification (structural fallback used if absent)
dkimpy>=1.1.5
# optional — legacy .doc/.xls VBA macro extraction (presence-only detection used if absent)
oletools>=0.60.1
